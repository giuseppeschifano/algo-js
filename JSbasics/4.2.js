
var x = Math.floor((Math.random() * 10) + 1);

function rand10(x) {   
	return x;
}

console.log('Your random number is: ' + rand10(x));


// first we need to declare a variable x
// then a function 'rand10' that contains the variable we just declared
// in the return we specify that it is equal to x
// at the end of our script we display the result with the console.log




